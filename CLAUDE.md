# 집콕맵 (apt-map) 프로젝트 안내

컴퓨터 세션과 폰 세션 등 여러 Claude Code 세션이 같은 디스크의 이 폴더를 오가며 작업합니다.
위스키/블로그 프로젝트(`D:/00.py/whisky_hot`, `D:/00.py/free_soft`, `D:/00.py/issue_blog`)와는 완전히 별개이며,
그쪽 파일은 절대 건드리지 않습니다. Snowflake, Airflow, Docker 도 쓰지 않습니다.

## 세션 시작/종료 시 꼭 할 일 (충돌 방지)

1. **시작 전**: `HANDOFF.md` 의 "지금 작업 중" 표를 먼저 읽고, 다른 세션이 잡고 있는 파일/작업은 건드리지 않는다.
   `git log --oneline | head` 와 `git status` 로 최근 커밋과 미커밋 변경도 확인한다.
2. **작업 시작하면**: "지금 작업 중" 표에 `세션(컴퓨터/폰) · 시작시각 · 작업명 · 만지는 파일` 한 줄을 추가한다.
3. **작업 단위가 끝날 때마다** 바로 `git commit` (작은 단위로 자주). 미커밋 상태로 오래 두지 않는다.
4. **끝나면**: "지금 작업 중" 표에서 내 줄을 지우고, "최근 업데이트"에 날짜/세션과 한 일을 짧게 적고,
   "진행 중 / 남은 작업"을 최신으로 고친다.
5. 다른 세션의 미커밋 변경(`git status`에 보이는데 내가 만든 게 아닌 것)은 되돌리거나 덮어쓰지 않는다.

## 데이터/키 규칙

- 키는 전부 `.env` (gitignore). `.env.example` 에 키 이름만 적는다. 키 값은 채팅에 다시 붙이지 않는다.
- 외부 호출은 캐시를 먼저 본다: `data/raw/` 에 이미 있으면 재수집하지 않는다 (카카오 일 한도, 학교알리미 WAF 차단).
- 네이버부동산 크롤링 금지(약관·소송 사례), 캡차 우회 금지, 학교알리미 `/ei/pp/` 파이썬 직접 호출 금지(WAF).
- 실행 순서와 스크립트 설명은 `README.md`. 앱 로컬 실행: `python -m http.server 8765 --directory app`.

## 코드 규칙

- 파이썬은 3.7 호환 (walrus, f-string `=` 등 금지). 의존성은 requests + scipy(보로노이)만.
- `pipeline/build.py` 가 유일한 산출물 생성기. 앱은 `app/data/*.json` 만 읽는 정적 사이트.
- 앱 정적 파일을 고치면 `index.html` 의 `?v=N` 캐시버스터를 올린다.
